=Completing the last task before a separator removes the separator=
# Use test: "login"
# Navigate to tasks page: /tasks/
# Select "Edit"
# Replace the entire content with exactly the following wikitext:
<syntaxhighlight lang="text">
=WAITING FOR=
* finish the last task before the separator
-----
* keep this waiting task
=NEXT ACTION=
* keep this next action
</syntaxhighlight>
# Select "Save"
# For the item "finish the last task before the separator", select "Check this task off the list"
# Select "Edit"
# Validate that the content exactly matches the following wikitext, with the completed task and the separator that would directly follow the heading removed - if not fail the test
<syntaxhighlight lang="text">
=WAITING FOR=
* keep this waiting task
=NEXT ACTION=
* keep this next action
</syntaxhighlight>
