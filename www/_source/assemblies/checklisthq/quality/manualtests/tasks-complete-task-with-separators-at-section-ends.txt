=Completing a task removes separators at section ends=
# Use test: "login"
# Navigate to tasks page: /tasks/
# Select "Edit"
# Replace the entire content with exactly the following wikitext:
<syntaxhighlight lang="text">
=WAITING FOR=
* finish this waiting task
----
=NEXT ACTION=
* keep this next action
----
</syntaxhighlight>
# Select "Save"
# For the item "finish this waiting task", select "Check this task off the list"
# Select "Edit"
# Validate that the content exactly matches the following wikitext, with the separator at the end of "Waiting For" and the separator at the end of the entire task list removed - if not fail the test
<syntaxhighlight lang="text">
=WAITING FOR=
=NEXT ACTION=
* keep this next action
</syntaxhighlight>
