=Completing a task preserves a preformatted section after a separator=
# Use test: "login"
# Navigate to tasks page: /tasks/
# Select "Edit"
# Replace the entire content with exactly the following wikitext:
<syntaxhighlight lang="text">
=NEXT ACTION=
* finish this task
----
<pre>
details after the separator
</pre>
</syntaxhighlight>
# Select "Save"
# For the item "finish this task", select "Check this task off the list"
# Select "Edit"
# Validate that the content exactly matches the following wikitext, with only the completed item removed and the separator and following preformatted section preserved unchanged - if not fail the test
<syntaxhighlight lang="text">
=NEXT ACTION=
<pre>
details after the separator
</pre>
</syntaxhighlight>
