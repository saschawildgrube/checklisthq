=Completing a task preserves a preformatted section under the next heading=
# Use test: "login"
# Navigate to tasks page: /tasks/
# Select "Edit"
# Replace the entire content with exactly the following wikitext:
<syntaxhighlight lang="text">
=NEXT ACTION=
* finish this task
=Example=
<pre>
details under the example heading
</pre>
</syntaxhighlight>
# Select "Save"
# For the item "finish this task", select "Check this task off the list"
# Select "Edit"
# Validate that the content exactly matches the following wikitext, with only the completed item removed and the next heading and its preformatted section preserved unchanged - if not fail the test
<syntaxhighlight lang="text">
=NEXT ACTION=
=Example=
<pre>
details under the example heading
</pre>
</syntaxhighlight>
